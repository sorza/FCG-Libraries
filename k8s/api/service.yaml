apiVersion: v1
kind: Service
metadata:
  name: libraries-api
  namespace: libraries
  labels:
    app: libraries-api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: libraries-api
