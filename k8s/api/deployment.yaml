apiVersion: apps/v1
kind: Deployment
metadata:
  name: libraries-api
  namespace: libraries
  labels:
    app: libraries-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: libraries-api
  template:
    metadata:
      labels:
        app: libraries-api
    spec:
      containers:
      - name: libraries-api
        image: alexandresorza/fcg-libraries-api:latest
        ports:
        - containerPort: 8080
        volumeMounts:
        - name: secrets-store
          mountPath: "/mnt/secrets-store"
          readOnly: true
        env:
        - name: ASPNETCORE_ENVIRONMENT
          valueFrom:
            configMapKeyRef:
              name: fcg-config
              key: ASPNETCORE_ENVIRONMENT
        - name: ApplicationInsights__ConnectionString
          valueFrom:
            secretKeyRef:
              name: fcg-secrets
              key: appinsights-connection-string
        - name: ConnectionStrings__DefaultConnection
          valueFrom:
            secretKeyRef:
              name: fcg-secrets
              key: sql-libraries-connection
        - name: ServiceBus__ConnectionString
          valueFrom:
            secretKeyRef:
              name: fcg-secrets
              key: servicebus-connection
        - name: ServiceBus__Topics__Libraries
          valueFrom:
            configMapKeyRef:
              name: fcg-config
              key: TOPIC_LIBRARIES
        - name: MongoSettings__ConnectionString
          valueFrom:
            secretKeyRef:
              name: fcg-secrets
              key: cosmosdb-connection
        - name: MongoSettings__Database
          value: "EventStoreDb"
        - name: MongoSettings__Collection
          value: "Events"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
      volumes:
      - name: secrets-store
        csi:
          driver: secrets-store.csi.k8s.io
          readOnly: true
          volumeAttributes:
            secretProviderClass: "fcg-keyvault-secrets"
---
apiVersion: v1
kind: Service
metadata:
  name: libraries-api-service
  namespace: libraries
spec:
  selector:
    app: libraries-api
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: ClusterIP

